# Retired module
# This module is intentionally empty. It can be used as a
# safe target for stacks that have been retired, allowing
# `terraform init`/`plan` to succeed with no changes.

terraform {
  required_version = ">= 1.3.0"
}
